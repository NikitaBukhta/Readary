# Template for secrets/api_keys.env (the real file is gitignored).
# Copy this file to api_keys.env and fill in your own credentials, then run
# `python bootstrap.py compile`. Missing values are fine — the Google Books
# search falls back to the anonymous (shared, lower) API quota.

GOOGLE_BOOKS_API_KEY=
GOOGLE_OAUTH_CLIENT_ID=
